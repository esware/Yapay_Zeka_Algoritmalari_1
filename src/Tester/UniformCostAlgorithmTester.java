package Tester;import javax.swing.*;import java.util.*;import Algorithms.*;import Simulations.*;public class UniformCostAlgorithmTester{    public static void main(String[] args)    {        Map<Environment, UniformCostSearch> environments = createTestEnvironments();        for (Map.Entry<Environment, UniformCostSearch> entry : environments.entrySet())        {            Environment env = entry.getKey();            UniformCostSearch geneticAlgorithm = entry.getValue();            env.printEnvironment();            // Her ortam için yeni bir Robot oluştur            Robot robot = new Robot(geneticAlgorithm,env);            // Algoritmanın başlama zamanını kaydet            long startTime = System.nanoTime();            // Her ortam için yeni bir Simulator oluştur            var simulator = new Simulator(false, robot, env);            // Simülasyonu çalıştır            int[] path = robot.startSearching(simulator);            // Algoritmanın bitiş zamanını kaydet            long endTime = System.nanoTime();            // Sonuçları raporla            reportResults(env, path, geneticAlgorithm, startTime, endTime);            // Simulator ve Robot nesnelerini temizle veya sıfırla            env.printEnvironment();            robot.setPosition(0,9);        }    }    private static  Map<Environment, UniformCostSearch> createTestEnvironments() {        Map<Environment, UniformCostSearch> environments = new HashMap<>() {};        var defaultEnvironment = new Environment(Environments.environment);        var lessComplexEnvironment = new Environment(Environments.Less_Complex_environment);        var midComplexEnvironment = new Environment(Environments.Mid_Complex_environment);        var highComplexEnvironment = new Environment(Environments.High_Complex_environment);        var ultraComplexEnvironment = new Environment(Environments.Ultra_Complex_environment);        environments.put(defaultEnvironment,new UniformCostSearch(defaultEnvironment)); // default environment        environments.put(lessComplexEnvironment,new UniformCostSearch(lessComplexEnvironment)); // less complex environment        environments.put(midComplexEnvironment,new UniformCostSearch(midComplexEnvironment)); // mid complex environment        environments.put(highComplexEnvironment,new UniformCostSearch(highComplexEnvironment)); // high complex environment        environments.put(ultraComplexEnvironment,new UniformCostSearch(ultraComplexEnvironment)); // ultra complex environment        return environments;    }    private static void reportResults(Environment env, int[] path, UniformCostSearch ga, long startTime , long endTime)    {        SwingUtilities.invokeLater(new Runnable() {            public void run() {                ResultFrame resultFrame = new ResultFrame("UniformCostSearch Algorithm ", env.environmentName.toString());                resultFrame.reportUniformCostSearchResult(env, path, ga, startTime, endTime);                resultFrame.setVisible(true); // Pencereyi görünür yap            }        });    }}